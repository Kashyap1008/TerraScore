import type { ScoreResponse } from './types';
import MOCK_JSON from '../../../fixtures/mock_score_response.json';

export const MOCK_SCORE_RESPONSE: ScoreResponse = MOCK_JSON as unknown as ScoreResponse;
